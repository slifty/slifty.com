<div class="tags-header">
  <h1>Tag: {{ tagData[0] }}</h1>
  <p>{{ tagData[1].length }} post{% if tagData[1].length != 1 %}s{% endif %} tagged with "{{ tagData[0] }}"</p>
</div>

<section class="category-posts">
  {% for post in tagData[1] | reverse %}
    <article class="archive-item">
      <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>

      <div class="post-meta">
        {% if post.data.date %}
          <time datetime="{{ post.data.date | htmlDateString }}">
            {{ post.data.date | readableDate }}
          </time>
        {% endif %}

        {% if post.data.categories %}
          <span class="post-categories">
            Categories:
            {% for category in post.data.categories %}
              <a href="/category/{{ category | slugify }}/">{{ category }}</a>{% if not loop.last %}, {% endif %}
            {% endfor %}
          </span>
        {% endif %}
      </div>

      {% if post.data.excerpt %}
        <p class="post-excerpt">{{ post.data.excerpt }}</p>
      {% endif %}
    </article>
  {% endfor %}
</section>
