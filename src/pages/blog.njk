---
layout: layouts/base.njk
title: Blog Archives
description: All posts from Sorry for the Spam
permalink: /blog/index.html
---

<div class="archives-header">
  <h1>{{ title }}</h1>
  <p>{{ collections.posts.length }} posts spanning 2010-2024</p>
</div>

<div class="content-with-sidebar">
  <div class="main-content">
    <section class="archives-list">
      {% for post in collections.posts %}
        <article class="archive-item">
          {% if post.data.featuredImage %}
            <a href="{{ post.url }}" class="archive-featured-image">
              <img src="{{ post.data.featuredImage }}" alt="{{ post.data.title }}">
            </a>
          {% endif %}

          <div class="archive-content">
            <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>

            <div class="post-meta">
              {% if post.data.date %}
                <time datetime="{{ post.data.date | htmlDateString }}">
                  {{ post.data.date | readableDate }}
                </time>
              {% endif %}

              {% if post.data.categories %}
                <span class="post-categories">
                  {% for category in post.data.categories %}
                    <a href="/category/{{ category | slugify }}/">{{ category }}</a>{% if not loop.last %}, {% endif %}
                  {% endfor %}
                </span>
              {% endif %}
            </div>

            {% if post.data.excerpt %}
              <p class="post-excerpt">{{ post.data.excerpt }}</p>
            {% endif %}
          </div>
        </article>
      {% endfor %}
    </section>
  </div>

  <aside class="pinned-posts">
    <h2>READ THESE NOW OR ELSE</h2>
    <ul class="pinned-posts-list">
      {% for post in collections.pinnedPosts %}
        <li class="pinned-post-item">
          <a href="{{ post.url }}" class="pinned-post-link">
            {% if post.data.featuredImage %}
              <img src="{{ post.data.featuredImage }}" alt="{{ post.data.title }}" class="pinned-post-thumb">
            {% endif %}
            <h3>{{ post.data.title }}</h3>
          </a>
        </li>
      {% endfor %}
    </ul>
  </aside>
</div>
